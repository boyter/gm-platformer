/// @desc DrawSetText(color, font, valign, haligh)
/// @arg colour
/// @arg font
/// @arg halign
/// @arg valign

// Allows one line setup for text avoiding mistakes and wondering why it didn't work
draw_set_color(argument0);
draw_set_font(argument1);
draw_set_halign(argument2);
draw_set_valign(argument3);