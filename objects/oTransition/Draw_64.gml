/// @description Draw black bars

// Draw GUI ignores the camera, so it will always draw visible to the player
if (mode != TRANS_MODE.OFF) 
{
	draw_set_color(c_black);
	draw_rectangle(0, 0, w, percent, )
}
